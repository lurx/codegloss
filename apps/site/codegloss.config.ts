import { defineConfig } from 'codegloss/config';

export default defineConfig({
	theme: 'github-light',
	darkTheme: 'github-dark',
	arcs: {
		strokeDasharray: '0',
		strokeWidth: 2,
		opacity: 0.65,
	},
});

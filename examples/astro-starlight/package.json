{
	"name": "@codegloss-examples/astro-starlight",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"copy-runtime": "node ./scripts/copy-runtime.mjs",
		"build": "pnpm copy-runtime && astro build",
		"dev": "pnpm copy-runtime && astro dev",
		"test": "pnpm copy-runtime && astro build && node ./test.mjs"
	},
	"dependencies": {
		"@astrojs/starlight": "^0.30.0",
		"astro": "^5.0.0",
		"codegloss": "workspace:*",
		"sharp": "^0.33.5"
	}
}

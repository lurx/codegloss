<script>
  // Side-effect import: registers the <code-gloss> custom element on the
  // client. Equivalent to the React/Vue examples that put the loader in their
  // root layout.
  import 'codegloss';
</script>

<slot />

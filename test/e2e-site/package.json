{
	"name": "@codegloss/e2e-site",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"test": "playwright test",
		"test:headed": "playwright test --headed",
		"test:ui": "playwright test --ui",
		"install-browsers": "playwright install --with-deps"
	},
	"devDependencies": {
		"@axe-core/playwright": "^4.11.1",
		"@playwright/test": "1.59.1"
	}
}
